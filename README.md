# Mini ledger commands tool

install dependencies
```
npm install

<browserify>
<tsc compiler>
```

to run
```
tsc index.ts ledger.ts
browserify index.js ledger.js -o bundle.js
```

Only modify typescript files
